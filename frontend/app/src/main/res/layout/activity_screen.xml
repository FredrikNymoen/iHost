<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#60a5fa"
    tools:context="no.ntnu.prog2007.ihost.SplashActivity">

    <!-- Container for iHost text - centered -->
    <LinearLayout
        android:id="@+id/textContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="horizontal">

        <!-- Letter i (without dot) -->
        <TextView
            android:id="@+id/letter_i"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ı"
            android:textColor="@color/splash_dark_brown"
            android:textSize="120sp"
            android:fontFamily="sans-serif"
            android:textStyle="bold"
            android:alpha="0" />

        <!-- Letter H -->
        <TextView
            android:id="@+id/letter_H"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="H"
            android:textColor="@color/splash_dark_brown"
            android:textSize="120sp"
            android:fontFamily="sans-serif"
            android:textStyle="bold"
            android:alpha="0" />

        <!-- Letter o -->
        <TextView
            android:id="@+id/letter_o"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="o"
            android:textColor="@color/splash_dark_brown"
            android:textSize="120sp"
            android:fontFamily="sans-serif"
            android:textStyle="bold"
            android:alpha="0" />

        <!-- Letter s -->
        <TextView
            android:id="@+id/letter_s"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="s"
            android:textColor="@color/splash_dark_brown"
            android:textSize="120sp"
            android:fontFamily="sans-serif"
            android:textStyle="bold"
            android:alpha="0" />

        <!-- Letter t -->
        <TextView
            android:id="@+id/letter_t"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="t"
            android:textColor="@color/splash_dark_brown"
            android:textSize="120sp"
            android:fontFamily="sans-serif"
            android:textStyle="bold"
            android:alpha="0" />

    </LinearLayout>

    <!-- Dot for the letter i -->
    <View
        android:id="@+id/dotOnI"
        android:color="@color/splash_dark_brown"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="@drawable/splash_screen_background"
        android:alpha="0"
        android:layout_alignTop="@id/textContainer"
        android:layout_alignStart="@id/textContainer"
        android:layout_marginTop="35dp"
        android:layout_marginStart="5dp" />

    <!-- Replay button -->
</RelativeLayout>